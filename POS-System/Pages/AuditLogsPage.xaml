<Page x:Class="POS_System.Pages.AuditLogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource Bg}"
      Foreground="{StaticResource TextBrush}" Opacity="0">


    
    <Page.RenderTransform>
        <TranslateTransform Y="8"/>
    </Page.RenderTransform>

    <Page.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.18"/>
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                     From="8" To="0" Duration="0:0:0.18"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

<Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="سجل العمليات" FontSize="21" FontWeight="Bold"/>
                    <TextBlock Text="بحث بالاسم / الفرع / الأكشن" Opacity="0.75" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="SearchBox" Width="320" Margin="0,0,10,0"
                             Style="{StaticResource DarkTextBoxStyle}"
                             TextChanged="SearchBox_TextChanged"
                             ToolTip="Search: Username / Branch / Action"/>
                    <Button Content="Refresh" Style="{StaticResource PrimaryButtonStyle}" Click="Refresh_Click"/>

                    <DatePicker x:Name="FromDate" Width="140" Margin="0,0,10,0"
                                SelectedDateChanged="FromDate_SelectedDateChanged"/>

                    <DatePicker x:Name="ToDate" Width="140" Margin="0,0,10,0"
                                SelectedDateChanged="ToDate_SelectedDateChanged"/>

                    <Button Content="مسح المدى" Margin="0,0,10,0" Style="{StaticResource DarkButtonStyle}" Click="DeleteRange_Click"/>
                    <Button Content="مسح الكل" Margin="0,0,10,0" Style="{StaticResource DarkButtonStyle}" Click="DeleteAll_Click"/>

                </StackPanel>
            </Grid>
        </Border>

        <!-- List -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <ListView x:Name="LogsList" 
                      Style="{StaticResource DarkListViewStyle}" 
                      ItemContainerStyle="{StaticResource DarkGridViewItemStyle}"
                      SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="At (UTC)" DisplayMemberBinding="{Binding AtUtc}" Width="190"/>
                        <GridViewColumn Header="User" DisplayMemberBinding="{Binding Username}" Width="160"/>
                        <GridViewColumn Header="Branch" DisplayMemberBinding="{Binding BranchName}" Width="180"/>
                        <GridViewColumn Header="Action" DisplayMemberBinding="{Binding Action}" Width="180"/>
                        <GridViewColumn Header="Details" DisplayMemberBinding="{Binding Details}" Width="420"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Page>