<Window x:Class="POS_System.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تسجيل الدخول"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#0B1220"
        FlowDirection="{Binding Source={StaticResource Loc}, Path=FlowDirection}"
        Language="{Binding Source={StaticResource Loc}, Path=Language}">

    <Grid>
        <!-- خلفية -->
        <Border Background="#0B1220"/>

        <!-- Top bar بسيط -->
        <Grid VerticalAlignment="Top" Height="64" Background="#0B1220">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="نظام المبيعات"
                       Foreground="#E5E7EB"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"/>

        </Grid>

        <!-- Card -->
        <Border Width="480"
                CornerRadius="22"
                Background="#111827"
                BorderBrush="#1F2937"
                BorderThickness="1"
                Padding="34"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>
                <TextBlock Text="أهلاً 👋"
                           Foreground="White"
                           FontSize="28"
                           FontWeight="Bold"/>

                <TextBlock Text="ادخل بياناتك عشان نفتح النظام"
                           Foreground="#9CA3AF"
                           Margin="0,8,0,26"/>

                <TextBlock Text="اسم المستخدم" Foreground="#E5E7EB" Margin="0,0,0,6"/>
                <Border CornerRadius="12" Background="#0B1220" BorderBrush="#1F2937" BorderThickness="1" Margin="0,0,0,16">
                    <TextBox x:Name="UserBox"
                             Background="Transparent"
                             Foreground="White"
                             CaretBrush="White"
                             SelectionBrush="#2563EB"
                             BorderThickness="0"
                             Padding="12"
                             FontSize="14"/>
                </Border>

                <TextBlock Text="كلمة المرور" Foreground="#E5E7EB" Margin="0,0,0,6"/>
                <Border CornerRadius="12" Background="#0B1220" BorderBrush="#1F2937" BorderThickness="1" Margin="0,0,0,16">
                    <PasswordBox x:Name="PassBox"
                                 Background="Transparent"
                                 Foreground="White"
                                 CaretBrush="White"
                                 SelectionBrush="#2563EB"
                                 BorderThickness="0"
                                 Padding="12"
                                 FontSize="14"/>
                </Border>

                <TextBlock x:Name="ErrorText"
                           Foreground="#F87171"
                           Margin="0,0,0,14"
                           TextWrapping="Wrap"/>

                <Button Content="دخول"
                        Height="46"
                        Background="#2563EB"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Login_Click"
                        IsDefault="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
