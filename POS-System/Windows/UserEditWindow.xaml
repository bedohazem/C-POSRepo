<Window x:Class="POS_System.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User" Width="460" Height="424"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Username" VerticalAlignment="Center"/>
        <TextBox x:Name="UserBox" Grid.Column="1" Height="34" Padding="10,6"/>

        <TextBlock Grid.Row="1" Text="Role" VerticalAlignment="Center" Margin="0,12,0,0"/>
        <ComboBox x:Name="RoleBox" Grid.Row="1" Grid.Column="1" Height="34" Margin="0,12,0,0"/>

        <TextBlock Grid.Row="2" Text="Branches" VerticalAlignment="Top" Margin="0,12,0,0"/>
        <Border Grid.Row="2" Grid.Column="1" Margin="0,12,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <ScrollViewer Height="90">
                <ItemsControl x:Name="BranchesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}"
                                      IsChecked="{Binding IsSelected}"
                                      Margin="10,6"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" Text="Active" VerticalAlignment="Center" Margin="0,12,0,0"/>
        <CheckBox x:Name="ActiveBox" Grid.Row="3" Grid.Column="1"
                  Margin="0,12,0,0"
                  Content="Active"
                  IsChecked="True"/>

        <TextBlock Grid.Row="4" Text="Password" VerticalAlignment="Center" Margin="0,12,0,0"/>
        <PasswordBox x:Name="PassBox" Grid.Row="4" Grid.Column="1" Height="34" Margin="0,12,0,0" Padding="10,6"/>

        <TextBlock x:Name="HintText" Grid.Row="5" Grid.ColumnSpan="2" Opacity="0.7" Margin="0,12,0,0"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,30,0,0"/>
        <Button Content="Save" Click="Save_Click" Margin="68,46,228,18" Grid.Row="5" Grid.ColumnSpan="2"/>
        <Button Content="Cancel" Margin="121,47,35,17" Click="Cancel_Click" Grid.Column="1" Grid.Row="5"/>
    </Grid>
</Window>
